name: Build and Deploy for gatsby
on:
  push:
    branches:
      - develop  
jobs:
  build-and-deploy:
    name: build and deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: npm install
        run: npm install
      - name: Add CNAME file
        run: |
          git config --global user.email "mymr0796@naver.com"
          git config --global user.name "kimseungyeon2"
      - name: gatsby build and deploy
        env:
          API_KEY: ${{ secrets.API_KEY_SEUNGYEON }}
        run: npm run deploy